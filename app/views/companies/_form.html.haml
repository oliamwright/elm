= semantic_form_for(@company) do |f|
	- f.inputs do
		= f.input :name
		= f.input :address
		= f.input :apt
		= f.input :city
		= f.input :state, :as => :select, :collection => Company::US_STATES
		= f.input :country
		= f.input :zip
		= f.submit
